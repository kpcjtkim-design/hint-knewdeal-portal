# 출결 입력 베타 (2026-09-11)

브랜치: `beta/attendance-input-20260911`

기준 main: `71f4999ccd856f8b176440303f9e2b6347379838` (macOS 실행 지원 포함).
운영 main과 운영 URL로 병합하거나 배포하지 않는다.

## 화면과 저장

- 관리자 출결대조에 출결 드롭다운을 추가했다. 해당없음·공란에서 처음 입력할 때는 확인창 없이 즉시 저장한다. 이미 정해진 출결 변경에는 확인창이 있다.
- 인정출석·인정지각·인정조퇴·인정외출을 포털에서 구분한다. 시트 값은 모두 인정출석이다.
- 사유는 직접 입력하고 사유 열 상단 '사유 저장'으로 저장한다. 기존 사유를 고칠 때만 확인한다. 입력 중인 사유는 다른 행의 상태 변경으로 사라지지 않는다.
- 서류제출은 매 변경 시 확인한다. 미해당=흰색, 미제출/반려=빨강, 확인=노랑. 미제출과 반려는 포털에서 별도로 보관한다.
- 세부 상태는 `settings/attendanceBeta_{classId}_{date}`에 보관하며, 대응 시트 값·색이 바뀌면 시트 상태를 우선한다.
- 서류 메모는 서류 드롭다운 아래 버튼으로 열며 기존 관리자 메모 및 후속조치 필드를 그대로 사용한다. 다른 학생 메모를 함께 덮어쓰지 않고 선택한 메모 필드만 저장한다.
- 체크히어 저장본의 입퇴실·판정·입실 메모·퇴실 메모·외출 구간을 서류제출 오른쪽에 표시한다. 그 다음 체크히어 메모, 수기출석 메모 순서다.
- 기본 날짜는 한국 시간 기준 오늘 또는 직전 교육일이다. 사유 폭은 145px, 전체 대조 영역은 최대 2560px이다.
- 담임 화면에는 입력 UI를 노출하지 않는다. 사용자의 명시적인 '담임에게도 출석바로바로 가능하게해' 지시 전 활성화하지 않는다. 담임 활성화 시 기존 admin-only reader와 포털 세부 상태 저장 규칙의 담당반 권한도 함께 적용·검증해야 한다.

## 개인 계정 시트 권한

'내 계정 시트 연결'은 현재 Firebase 사용자로 Google 재인증한다. 계정에 시트 편집 권한이 있어야 저장할 수 있다. 특정 공용 관리자 계정의 자격증명이나 서비스 계정으로 대신 쓰지 않는다.

요청 권한은 `spreadsheets`와 기존 Drive 메타데이터 읽기 권한이다. Google OAuth 앱의 검증 상태에 따라 미검증 앱 경고가 나올 수 있다. Google Sheets API 사용 설정과 미리보기 호스트의 Firebase Authorized domains 등록도 필요할 수 있다. 토큰은 페이지 메모리에만 보관한다.

대안: Google Picker와 `drive.file` 권한으로 사용자가 선택한 운영 파일만 허용하는 구조는 추후 추가할 수 있다. 이번 베타에는 Picker를 구현했다고 가정하지 않는다.

## 원본 보호 범위

- 반별 Firestore sheetUrl의 파일과 gid, 반 제목, 학생 이름, 출결 및 가-3 날짜를 확인한다.
- 기존 reader와 같은 M18 출결 헤더, M19:M48 명단, M50 가-3 날짜, 51행 사유 구조를 검증한다. 이름·날짜가 중복되거나 구조가 다르면 중단한다.
- 쓰기 요청은 단일 셀 `updateCells`이며 값 또는 배경색 필드만 지정한다. 시트/행/열/파일 삭제나 전체 범위 초기화 요청은 없다. 값은 stringValue로 저장해 수식으로 실행하지 않는다. 수식 셀은 수정하지 않는다.
- 저장 전 기존 값과 학생·날짜를 확인하고, 저장 후 다시 읽어 값이 일치해야 성공으로 표시한다. 결과 미확인은 자동 재시도하지 않는다.
- 가-3는 기존 parser를 재사용한다. 확실한 학생별 항목만 수정하고, 명확한 공통 사유는 학생별로 나눈다. 이름 반복이나 불명확한 혼합 문장은 저장을 중단한다.
- Sheets API에는 이 읽기→쓰기 전체를 묶는 셀 조건부 트랜잭션이 없다. 외부에서 정확히 동시에 같은 셀을 고치는 경쟁 상태를 완전히 막지는 못하므로 베타에서는 같은 반·날짜를 여러 사람이 동시에 수정하지 않는 운영 검증이 필요하다. 개인 계정 권한만으로 프로그램 오류가 불가능해지는 것은 아니다.
- 포털 세부 구분 저장이 실패했지만 시트 저장이 성공한 경우를 별도로 안내한다.

## 17개 반 수집

체크히어 탭에서 '전체반 · 8/27~오늘'을 선택하고 '체크히어에서 수집'을 누른다. 기존 수집 서버 프로토콜을 사용하므로 Windows/macOS 지원을 유지한다. 하나의 반·날짜씩 순서대로 읽고, 없는 강의·일부 실패를 목록으로 남겨 다음 날짜를 계속 처리한다.

페이지와 수집 프로그램을 열어둬야 한다. 탭을 닫아도 계속되는 예약 작업은 아니다. 전체 기간을 매 시간 다시 읽으면 학생별 상세 창을 여는 수가 많으므로 실측 처리시간을 확인해야 한다. 실제 500명 전체 수집을 이번 개발 검사에서 실행하지 않았다.

완료된 수집은 기존 자동 보관을 유지하며 '플랫폼에 저장'으로 선택한 전체 반·기간을 다시 보관할 수도 있다. 출결대조는 해당 반·날짜 저장본만 읽는다. 저장 순서 대신 실제 collectedAt 기준으로 최신 학생 자료를 선택하고, 동명이인은 자동 연결하지 않는다.

## 검증

가상 Google Sheets 응답 및 실제 브라우저 fixture에서 최초 입력/정정/취소, 사유 저장 시점, 서류 확인창, 세부 상태 재조회, 관리자 메모, 저장본 표시를 검사한다. 단일 셀 범위, 다른 필드 보존, 개인 편집 권한 없음, 수식 보호, 동시 변경 감지, 응답 유실 후 검증, 전체 수집의 일부 날짜 실패도 검사한다.

실제 운영 시트의 출결이나 사유·색을 개발 시험으로 변경하지 않는다. 실제 OAuth 승인과 원본 저장은 사용자 베타 검증 단계에서 구분해 확인한다.

참고: https://developers.google.com/workspace/sheets/api/scopes
https://developers.google.com/identity/protocols/oauth2/production-readiness/sensitive-scope-verification
https://firebase.google.com/docs/auth/web/google-signin
